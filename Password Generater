#simple python program that let u to generate password 
#u can also choose how many letter should it be
import random
chars = 'abcdefghijklmnopqrstuvwxyz1234567890!@#$%^&*'
length = int(input('password length?:'))
password = ''
for c in range(length):
    password += random.choice(chars)

print(password)
